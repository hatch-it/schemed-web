import React, { Component } from 'react'

class DayInfo extends Component {
    constructor(props) {
        super(props);
        this.state = {
            info: this.state.availabilities,

        }
    }
    printInfo = day => {
        for(var i = 0; i < info.length; i++) {
            if(info[i][start].toDateString() === day.toDateString()) {
            }
        }
    }
}